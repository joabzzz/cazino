// Configure API endpoints
// Leave empty to use same origin (for local development)
// Or set to deployed API URL (for production)
window.CAZINO_CONFIG = {
  apiBase: "https://cazino-api.joabara.workers.dev", // For production
  wsUrl: "wss://cazino-api.joabara.workers.dev", // For production

  // For local development, comment out above and use:
  // apiBase: "", // Uses localhost
  // wsUrl: "", // Uses localhost
};
